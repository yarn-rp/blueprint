# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on main merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    uses: ./.github/workflows/firebase-hosting-live.yml
    with:
      project_id: blueprint-384623
      flavor: production
      target: lib/main_production.dart
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PRD }}

