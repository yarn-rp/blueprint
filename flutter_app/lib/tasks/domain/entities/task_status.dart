/// Represents the status of the task. This is going to depend on the provider.
class TaskStatus {
  TaskStatus(
    this.status,
    this.hexColor,
  );

  final String status;
  final String hexColor;
}
